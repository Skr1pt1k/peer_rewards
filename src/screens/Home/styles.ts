import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    addBtn: {
        position: 'absolute',
        bottom: 40,
        right: 30,
        width: 60,
        height: 50,
        borderRadius: 23,
    },
    addImage: {
        width: 60,
        height: 50
    }
})